[phases.setup]
nixPkgs = ["nodejs_20", "python311", "python311Packages.pip", "bash"]

[phases.install]
cmds = [
  "npm ci",
  "cd ai-service && pip install -r requirements.txt"
]

[phases.build]
cmds = ["chmod +x start-railway.sh"]

[start]
cmd = "bash start-railway.sh"
